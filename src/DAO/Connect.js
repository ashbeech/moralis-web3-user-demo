import Account from "../DAO/Account";

import { Container, Box } from "@chakra-ui/react";

const Connect = () => {
  return (
    <Container>
      <Box>
        <Account />
      </Box>
    </Container>
  );
};

export default Connect;
